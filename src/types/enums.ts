export enum AudioPlayerState {
	NONE = "none",
	READY = "ready",
	PLAYING = "playing",
	PAUSED = "paused",
	ENDED = "ended",
	LOADING = "loading",
	BUFFERING = "buffering",
	ERROR = "error",
}

export enum SongRating {
	NO_RATING = "noRating",
	FAVOURITE = "favourite",
}
